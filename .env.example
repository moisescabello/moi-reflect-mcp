# Reflect access token
# Generate it at https://reflect.app/developer
REFLECT_TOKEN=your_token_here

# Graph ID
# Retrieve it via the /api/graphs endpoint with your token
GRAPH_ID=your_graph_id_here

# Optional timezone (IANA format) for daily note helpers, e.g. Europe/Madrid
# Defaults to the system timezone if omitted.
# REFLECT_TIMEZONE=Europe/Madrid
